<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="az.cybernet.internship.dictionary.mapper.DictionaryMapper">

    <select id="findAllActiveDictionaryWithLimit" resultType="az.cybernet.internship.dictionary.model.Dictionary">
        SELECT *
        FROM dictionary
        <where>
            <if test="value != null and value != ''">
                AND LOWER(value) LIKE CONCAT('%', #{value}, '%')
            </if>
            <if test="isActive != null">
                AND is_active = #{isActive}
            </if>
        </where>
        LIMIT #{limit}
    </select>

    <update id="restore">
        UPDATE dictionary
        SET
            is_active = TRUE,
            deleted_at = NULL
        WHERE id = #{id}
    </update>

    <select id="findById" resultType="az.cybernet.internship.dictionary.model.Dictionary">
        SELECT * FROM dictionary WHERE id = #{id}
    </select>

    <update id="update" parameterType="az.cybernet.internship.dictionary.model.Dictionary">
        UPDATE dictionary
        SET category = #{category},
            value = #{value},
            description = #{description},
            is_active = #{isActive},
            deleted_at = #{deletedAt}
        WHERE id = #{id}
    </update>

    <insert id="insert" parameterType="az.cybernet.internship.dictionary.model.Dictionary">
        INSERT INTO dictionary (id, category, value, description, is_active)
        VALUES (#{id}, #{category}, #{value}, #{description}, #{isActive})
    </insert>

    <update id="updateForSoftDelete" parameterType="az.cybernet.internship.dictionary.model.Dictionary">
        UPDATE dictionary
        SET is_active = FALSE,
            deleted_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>
</mapper>